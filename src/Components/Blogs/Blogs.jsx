import React from "react";
import useTitle from "../../Hooks/useTitle";

const Blogs = () => {
  useTitle("Blogs");
  return (
    <>
      <section>
        <h1>Blos</h1>
      </section>
    </>
  );
};

export default Blogs;
